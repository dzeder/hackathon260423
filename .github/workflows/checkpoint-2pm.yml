name: Checkpoint 2pm
on:
  schedule:
    - cron: '0 19 * * *'  # 2:00 PM CDT = 19:00 UTC
  workflow_dispatch:

permissions:
  contents: write

jobs:
  checkpoint:
    uses: ./.github/workflows/_checkpoint.yml
    secrets: inherit
    with:
      time: '2pm'
